记录VIM编辑器下的批量注释功能：
1.块模式
添加注释：
（1）"v" 进入 virtual 模式
（2）选择需要注释的批量行数
（3）"ctrl + v" 进入列编辑模式（也可以直接从此进行选择）
（4）"I" 进入插入模式，输入注释符号"#", "ESC"按键退出（两次）

取消注释：
（1）"ctrl + v" 进入列编辑模式
（2）选择需要去除注释的行数（需要注意，如果删除"//",需要同步选择两个"/"）
（3）"d" 删除对应的注释
